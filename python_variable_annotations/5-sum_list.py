#!/usr/bin/env python3
"""
Module that defines annotated list with float values and return sum with float value
"""


def sum_list(input_list: list[float]) -> float:
    """Return the sum of a list of floats."""
    return sum(input_list)
